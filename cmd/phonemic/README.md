# PhoneMic — desktop binary

This is the Go module for the PhoneMic desktop app (gioui window + HTTPS/WebSocket server).

See the full documentation at the repository root:

- English: [`../../README.md`](../../README.md)
- 简体中文: [`../../README.zh-CN.md`](../../README.zh-CN.md)

Quick run:

```bash
go mod tidy
go run .
```
